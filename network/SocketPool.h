#pragma once
//SocketPool:20130125
#include <iostream>
#include <vector>
#include "Socket.h"
namespace neuria{
namespace network
{
	/*
class SocketPool{
public:
    SocketPool(){}
    ~SocketPool(){}

	auto Add(const Socket::Ptr socket) -> void {
		this->socket_array.push_back(socket);
	}

	auto Remove(const Socket::Ptr& socket_ptr) -> void {
		this->socket_array.erase(socket_ptr);
	}

private:
	std::vector<Socket::Ptr> socket_array;
};
}
*/
}
